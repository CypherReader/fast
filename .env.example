# Database Connection
DSN=root:password@tcp(localhost:3306)/fastinghero?parseTime=true

# Security
# Generate a secure secret using: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-me

# Third Party APIs
DEEPSEEK_API_KEY=your-deepseek-api-key
